export class RecoveryService {
  // Placeholder (restore from tar, validate integrity)
  async restoreFromArchive(_path: string): Promise<void> {
    // Intentionally minimal; see scripts/daemon-recovery.sh
  }
}

