/**
 * Example TypeScript file for validation testing
 */

export function saludar(nombre: string): string {
  return `Hola, ${nombre}!`;
}

export function calcularAreaCirculo(radio: number): number {
  return Math.PI * radio * radio;
}
